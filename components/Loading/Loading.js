
import { HashLoader } from "react-spinners";

const Loading = () => {
    return (
        <div className="h-screen -mt-40 flex justify-center items-center">
           <HashLoader color="#20f7c4" />

        </div>
    );
};

export default Loading;